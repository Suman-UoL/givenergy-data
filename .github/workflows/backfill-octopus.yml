name: Backfill Octopus Data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - name: Run backfill
        env:
          OCTOPUS_API_KEY: ${{ secrets.OCTOPUS_API_KEY }}
        run: python scripts/octopus_backfill.py
      - name: Commit data
        run: |
          git config user.name "givenergy-bot"
          git config user.email "bot@givenergy-data"
          git add data/octopus/
          git diff --cached --quiet && echo "No changes" || \
            git commit -m "chore: octopus backfill $(date -u '+%Y-%m-%d %H:%M UTC')" && \
            git push
